import React from "react";

const FormHeading = ({ title }) => {
  return (
    <h1 className="base-heading verify-heading weight-700 text-center">
      {title}
    </h1>
  );
};

export default FormHeading;
